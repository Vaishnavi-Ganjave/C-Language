#include<stdio.h>
#include<conio.h>
void main()
{
	int a[10],n,i;
	printf("enter elements to find their square: ");
	
	for(i=0;i<n;i++)
	{
		printf("square of element is %d: ",a[i]*a[i]);
		printf("%d");
	}
	
	getch();
}